namespace FrostWoodTech.API.Enums;

/// <summary>Course is first: EF omits a CLR-default value on insert, so it must match the column default.</summary>
public enum CertificateCategory
{
    Course,
    Exam
}
